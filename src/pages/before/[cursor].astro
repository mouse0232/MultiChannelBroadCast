---
import List from '../../components/list.astro'
import { getChannelInfo } from '../../lib/telegram'

const { cursor } = Astro.params

const channel = await getChannelInfo(Astro, { before: cursor })
---

<List channel={channel} />
