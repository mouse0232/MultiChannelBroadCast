version: '3.8'

services:
  app:
    build:
      context: .
      # 国内用户推荐使用 Dockerfile.cn (淘宝npm镜像,更快更稳定)
      dockerfile: Dockerfile.cn
      # dockerfile: Dockerfile
    image: multi-channel-broadcast:latest
    container_name: multi-channel-broadcast
    ports:
      - "4321:4321"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=4321
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
